using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Building : MonoBehaviour
{
	public Vector3 rotOffset, standardPos;

	// Update is called once per frame
	void Update ()
	{
		//transform.position = standardPos + rotOffset;
	}
}
